# ISSUE / タスク整理（要件定義フェーズからの流れ）

## 0. 方針
- スマホ専用 UI、PC でもモバイル幅表示。
- トップページ + 各ツールを個別ページ（Vue Router）。
- GitHub Pages 配信、PWA はベストエフォートで維持。

## 1. 要件定義（現フェーズ）
- [x] ユースケースと利用シナリオを文章化（誰が・いつ・何をするか）。
- [x] 各ツールの入力/出力を簡潔に定義（例: 点数計算=入力: 翻/符、出力: 支払い点）。
- [x] 非機能要件の具体化（パフォーマンス目標、オフライン挙動の範囲）。
- [x] 画面リストと遷移図（トップ/各ツール）※簡易リストを要件に記載。図面は後続で必要に応じて作成。
- [x] 完了条件: 関係者合意。

## 2. 基本設計
- [x] 画面ワイヤー（モバイル幅、1 カラム）。
- [x] ルーティング設計（path / lazy load / meta 情報）。
- [x] 共通レイアウト（ヘッダー/フッター/ナビ）。
- [x] 入力コンポーネント仕様（牌入力、スライダー、数値入力の制限、フォーカス/ARIA）。
- [x] テーマ切替方針（色トークン化とローカル保存）。

## 3. 詳細設計・実装
- [ ] Vue Router 導入、コンポーネントのページ分割。
- [ ] スタイルのモバイル最適化（max-width、余白、タップ領域）。
- [ ] 既存ツールコンポーネントを各ページへ配置・微調整。
- [ ] オフライン対応の BASE 対応確認（Service Worker と base の組み合わせ）。
- [ ] 単体テスト追加（`src/lib/mahjong.ts`: 待ち判定、点数計算）。

## 4. テスト
- [ ] 手動スモーク（全ページ遷移・主要操作）。
- [ ] CI で lint/typecheck/test/build を流す。
- [ ] 端末確認（iOS Safari/Android Chrome で表示崩れチェック）。

## 5. リリース
- [ ] Pages デプロイ動作確認（`base` と manifest/scope の整合性）。
- [ ] README にルーティング/操作ガイド追記。
- [ ] バージョンタグ付与（任意）。

## 補足
- ISSUE は状況に応じて分割/細分化してください。優先順位は要件定義→ルーティング導入→モバイル最適化の順で進める。
